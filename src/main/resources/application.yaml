app:
  upload:
    image: /var/app/uploads/images
    document: /var/app/uploads/docs

spring:
  mail:
    host: smtp.gmail.com
    port: 587
    username: chanmyae.huawei@gmail.com
    password: hjgt sjbp kxqh ztoj
    properties:
      mail:
        smtp:
          auth: true
          starttls:
            enable: true

  application:
    name: ewsd-etutor-backend

  servlet:
    multipart:
      max-file-size: 25MB
      max-request-size: 25MB

  datasource:
    driverClassName: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/ewsd?useSSL=false&serverTimezone=Asia/Bangkok
    username: springuser
    password: StrongPassword123!

  jpa:
    defer-datasource-initialization: true
    hibernate:
      ddl-auto: validate # use 'validate' in production
    show-sql: true
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true

  sql:
    init:
      mode: always

  security:
    filter:
      order: 5

# JWT Configuration
jwt:
  secret: a-string-secret-at-least-256-bits-long
  expiration: 3600000  # 1 hour in milliseconds
  refresh:
    expiration: 604800000  # 7 days in milliseconds

# Server Configuration
server:
  port: 8080
  error:
    include-message: always